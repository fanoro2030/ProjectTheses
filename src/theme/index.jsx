import * as colors from '../../constants/colors';

const theme = createTheme({
  // Aquí puedes usar tus constantes de colores importadas
  palette: {
    primary: {
      main: colors.DARK_GREEN,
    },
    secondary: {
      main: colors.LIGHT_GREEN,
    },
  },
  // Resto de tus estilos
});
